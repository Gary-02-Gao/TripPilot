# 本地运行说明

首次克隆或下载后，请先按下方“环境重建与迁移”创建本目录的 `.venv`；虚拟环境不随 Git 仓库分发。

macOS 用户完成环境准备后，双击根目录的 **启动项目.command**。脚本会使用本目录 `.venv` 的 Python 3.12，启动后自动打开浏览器；不需要先手工激活环境。其他系统可使用 [核心代码中的标准库启动命令](03_核心代码/README.md)。

双击 **检查环境.command** 可检查解释器和依赖。每个目录有独立环境，互不依赖另一个项目的虚拟环境。

## TripPilot 交互界面

- 默认地址：http://127.0.0.1:18091；端口被其他程序占用时自动寻找后续空闲端口，并打印实际地址。
- 这是项目已有的离线工具交互演示；归档模型输出来自历史评测，不是实时大模型推理。
- 请保留启动终端，按 Ctrl+C 停止；重复双击会打开已运行的同一项目。
- 本启动环境只需 Python 标准库。训练、适配器合并和 GPU 推理属于独立运行条件，本次未下载基座或配置 CUDA 训练环境。

## 环境重建与迁移

`.venv` 是本机环境，移动目录或换电脑后应重建，不应作为投递文件打包。
在项目根目录执行（需先安装 uv）：

```bash
uv venv --python 3.12 .venv
uv pip sync --python .venv/bin/python .launcher/requirements.lock.txt
```

依赖清单为空的 TripPilot 演示只需第一行。

`MANIFEST.sha256` 校验当前提交版本的文件；`02_成果与验证/source-manifest.json` 保留原始源码哈希，并记录当前交付哈希与必要修改。
