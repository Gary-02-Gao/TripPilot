# 源码来源与开发历史核验

核验日期：2026-09-23。本记录区分本地保留的研究历史、后续代码快照和当前公开作品集，避免把重复副本或其他研究线的提交当作本项目完整开发过程。

## 检查到的资料

| 本地资料名称 | 核验结果 | 本次使用方式 |
| --- | --- | --- |
| `Agent+SFT` | `main` 保留 24 次提交，根提交 `3ec80c8`，末端 `19585a7` | 核对早期同源代码和研究记录；未原样导入公开仓库 |
| `Agent-SFT-private-submission` | 与上述仓库的 `main` 同一 HEAD、同一祖先 | 视为同一历史副本，不重复计数 |
| `Agent+SFT-2` | 无 `.git`，保留后续训练、演示与评测文件 | 核对代码与数据哈希；未导入完整数据、权重或日志 |
| `TripPilot_Agent_SFT_整理版` | 无 `.git`，为后续整理副本 | 交叉核对训练链和代码；未导入整包 |
| `TripPilot-LoRA-Portfolio` | 无 `.git`，精选代码多数与当前版本同源 | 摘取并简化三城市编码映射，补齐本地数据转换示例 |
| `TripPilot-Job-Application` | 无 `.git`，为个人求职材料 | 未引入 |
| 作品集 ZIP | 不含 `.git` | 不作为 Git 开发历史证据 |

## 原始 Git 历史的边界

早期 `main` 的 24 次提交集中在 2026-09-04 05:26:38–21:11:51（UTC+08:00），包含 planner QLoRA、数据契约、安全课程和约束修复等实际增量。它们不是当前 TripPilot 顺序化 v2、前端与评测实现的完整提交链。

`inspect_qwen_dataset.py`、`train_qwen_last_assistant_lora.py`、`merge_lora_into_base.py`、`conversation_splitter.py` 与旧仓库存在明确同源关系；例如数据检查脚本的 11/13 个函数 AST 完全一致。但这四个文件在早期历史中均由根提交导入，后续 23 次提交没有记录它们向当前版本的演进。核验时已有的 22 项原始文件哈希在该 Git 历史中均无精确匹配，因此不能把这段研究历史标记为最终交付代码的完整可复现历史。

旧历史包含本机路径等不适合原样公开的信息，并带有另一研究线的依赖和素材。内部工具检查点、第三方浅克隆和重复备份均不计为 TripPilot 开发提交。

## 本次实际补充

- 城市映射从原有 361 城配置中摘取嘉兴、杭州、上海，仅保留转换器使用的字段；原文件和当前文件分别记录哈希。
- 两条输入对话为新编写的公开合成样例，不包含原始训练数据或用户画像，也不用于证明历史模型指标。
- 新增 CLI 回归测试，核验两条输入生成三条监督前缀、攻略先于天气、城市参数规范化以及路线参数清理。
- 根 README 补齐安装、使用、配置、接口、测试、运行边界与许可；独立环境验证不需要外部模型或依赖下载。

公开仓库的 Git 日志反映作品集整理与后续实际修改。历史训练指标仍以原有归档证据为准；开发历史说明本身不证明任何外部平台的准入项已通过。
