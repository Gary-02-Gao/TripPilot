# 本地环境验证

核验日期：2026-09-21。

- 独立 Python 3.12.14 环境：`.venv`。
- 依赖一致性检查通过。
- `.command` 文件语法、执行权限及实际启动验证通过。
- 本次运行的 10 项离线测试通过。
- `/health`、聊天接口、评测数据和主页检查通过。
- 重复启动复用当前实例；本地端口冲突自动顺延的机制已验证。
- 测试进程已停止，双击启动文件可重新打开；本次未准备 GPU 训练环境。

机器可读结果保存在本机 `.runtime/environment-check.json`，不随仓库发布。2026-09-21 核验时浏览器自动化通道不可用，前端检查为 HTTP 与静态资源验证，未作新的浏览器交互回归。

## GitHub 提交前核验（2026-09-23）

- Python 3.12.14 下 **14/14 单元测试通过**，包含新增的异常模型输出和路线参数契约回归检查。
- 公开评测记录复算通过：378 个唯一节点、200 条对话，10 项计数与汇总一致。
- 16 个 Python 文件语法检查、9 个 JSON 文件解析、两个启动脚本语法检查通过。
- 本地 HTTP 验证通过：主页、静态资源、评测、健康检查、四类工作流与重置可用；非法 JSON、非法 UTF-8、错误输入类型和超长请求返回 400，隐藏文件和路径穿越请求返回 404。
- Safari 实机检查通过：首页、缺少城市时追问、补充杭州后攻略与天气依赖链、参数详情收起、LoRA 归档回放、评测指标展示和新对话重置。
- 启动器通过端口冲突顺延、重复启动复用和停止清理验证；复制到含中文与空格的新路径并重建虚拟环境后，两个启动脚本可用。
- 文本敏感信息扫描、截图审阅和媒体元数据检查未发现密钥或私人本机路径；本地虚拟环境、日志和配置文件不纳入提交。
- 本次修复评测 JSON 解析健壮性与严格匹配条件、路线调用参数和说明文案；历史训练记录及评测结果未改写。

本次核验覆盖可在当前环境运行的离线部分。未重新执行 GPU 训练、模型合并或完整模型推理；这些步骤仍需外部依赖、模型权重及数据。

## 文档与数据转换补充核验（2026-09-23）

- 将仓库交付文件复制到临时目录，使用现有 Python 3.12.14 创建全新 `.venv`，未下载第三方依赖；根 README 的离线启动、原有 14 项测试和评测计数复算通过。
- 实际 HTTP 请求核对健康检查、重置、嘉兴三日游、缺少城市时追问后补充杭州、酒店与评价链、路线和评测接口，结果符合文档。
- 核对内置 8090、配置样例 8091，以及命令行 > 环境变量 > `.env` > 默认值的优先级；临时服务和目录已清理。实测平台为 macOS，未执行 Windows PowerShell。
- 新增安全合成数据和三城市映射后，**15/15 单元测试通过**；新增测试实际执行转换 CLI，验证 2 条输入生成 3 条监督前缀及工具依赖与参数契约。
- 对相关旧项目的核验只补充来源说明与可运行示例；未将其他研究线、内部检查点或重复副本计为本项目历史。
